#!/bin/bash
source $( cd $( dirname ${BASH_SOURCE[0]} ) && pwd )/ice-skel-web
source $( cd $( dirname ${BASH_SOURCE[0]} ) && pwd )/ice-skel-service
source $( cd $( dirname ${BASH_SOURCE[0]} ) && pwd )/ice-skel-daemon
